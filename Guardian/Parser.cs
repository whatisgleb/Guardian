using System;
using Guardian.Interfaces;
using System.Collections.Generic;
using System.Linq;
using System.Linq.Expressions;

namespace Guardian
{
    //public class Parser
    //{
    //    private readonly IEnumerable<IStatement> _statements;
    //    private readonly Dictionary<int, IRule> _ruleDictionary;

    //    public Parser(IEnumerable<IStatement> statements, IEnumerable<IRule> rules) {

    //        _statements = statements;
    //        _ruleDictionary = rules.ToDictionary(rule => rule.ID, rule => rule);
    //    }

    //    public IEnumerable<Expression> GenerateExpressions() {

    //        var expressions = new List<Expression>();

    //        foreach (var statement in _statements) {
                
    //            expressions.Add(GenerateExpression(statement));
    //        }
    //    }

    //    public Expression GenerateExpression(IStatement statement) {
            

    //    }

    //    public Stack<>
    //}
}
